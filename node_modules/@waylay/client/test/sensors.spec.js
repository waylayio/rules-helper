/* eslint-env jest */

const Url = require('url')
const Waylay = require('../index')
const moxios = require('moxios')

const waylay = new Waylay({
  token: 'a-test-token',
  domain: 'my-domain.waylay.io'
})

beforeEach(() => moxios.install(waylay.httpClient))
afterEach(() => moxios.uninstall())

test('execute', done => {
  const { name, version, body } = {
    name: 'my-script',
    version: '1.0.0',
    body: { data: '0xBABE' }
  }

  waylay.sensors.execute(name, version, body)

  moxios.wait(() => {
    const { config, url } = moxios.requests.mostRecent()

    expect(config).toMatchObject({
      method: 'post',
      data: JSON.stringify(body)
    })
    expect(Url.parse(url)).toMatchObject({
      protocol: 'https:',
      host: 'my-domain.waylay.io',
      path: `/api/sensors/${name}/versions/${version}`
    })

    done()
  })
})

test('execute latest', done => {
  const { name, version, body } = {
    name: 'my-script',
    version: 'latest',
    body: { data: '0xBABE' }
  }

  waylay.sensors.execute(name, version, body)

  moxios.wait(() => {
    const { config, url } = moxios.requests.mostRecent()

    expect(config).toMatchObject({
      method: 'post',
      data: JSON.stringify(body)
    })
    expect(Url.parse(url)).toMatchObject({
      protocol: 'https:',
      host: 'my-domain.waylay.io',
      path: `/api/sensors/${name}`
    })

    done()
  })
})

test('list', done => {
  waylay.sensors.list()

  moxios.wait(() => {
    const { config, url } = moxios.requests.mostRecent()

    expect(config).toMatchObject({ method: 'get' })
    expect(Url.parse(url)).toMatchObject({
      protocol: 'https:',
      host: 'my-domain.waylay.io',
      path: '/api/sensors'
    })

    done()
  })
})

test('list', done => {
  waylay.sensors.list({ includeDeprecated: true })

  moxios.wait(() => {
    const { config, url } = moxios.requests.mostRecent()

    expect(config).toMatchObject({ method: 'get' })
    expect(Url.parse(url)).toMatchObject({
      protocol: 'https:',
      host: 'my-domain.waylay.io',
      path: '/api/sensors?includeDeprecated=true'
    })

    done()
  })
})

test('get', done => {
  const { name, version } = { name: 'my-script', version: '1.0.0' }
  waylay.sensors.get(name, version)

  moxios.wait(() => {
    const { config, url } = moxios.requests.mostRecent()

    expect(config).toMatchObject({ method: 'get' })
    expect(Url.parse(url)).toMatchObject({
      protocol: 'https:',
      host: 'my-domain.waylay.io',
      path: `/api/sensors/${name}/versions/${version}`
    })

    done()
  })
})

test('get latest', done => {
  const { name, version } = { name: 'my-script', version: 'latest' }
  waylay.sensors.get(name, version)

  moxios.wait(() => {
    const { config, url } = moxios.requests.mostRecent()

    expect(config).toMatchObject({ method: 'get' })
    expect(Url.parse(url)).toMatchObject({
      protocol: 'https:',
      host: 'my-domain.waylay.io',
      path: `/api/sensors/${name}`
    })

    done()
  })
})

test('get latest with params', done => {
  const { name } = { name: 'my-script' }
  waylay.sensors.get(name, { includeDeprecated: true })

  moxios.wait(() => {
    const { config, url } = moxios.requests.mostRecent()

    expect(config).toMatchObject({ method: 'get' })
    expect(Url.parse(url)).toMatchObject({
      protocol: 'https:',
      host: 'my-domain.waylay.io',
      path: `/api/sensors/${name}?includeDeprecated=true`
    })

    done()
  })
})

test('get version with params', done => {
  const { name, version } = { name: 'my-script', version: '1.0.0' }
  waylay.sensors.get(name, version, { includeDeprecated: true })

  moxios.wait(() => {
    const { config, url } = moxios.requests.mostRecent()

    expect(config).toMatchObject({ method: 'get' })
    expect(Url.parse(url)).toMatchObject({
      protocol: 'https:',
      host: 'my-domain.waylay.io',
      path: `/api/sensors/${name}/versions/1.0.0?includeDeprecated=true`
    })

    done()
  })
})

test('get versions with params', done => {
  const name = 'my-script'
  waylay.sensors.getVersions(name, { includeDeprecated: true })

  moxios.wait(() => {
    const { config, url } = moxios.requests.mostRecent()

    expect(config).toMatchObject({ method: 'get' })
    expect(Url.parse(url)).toMatchObject({
      protocol: 'https:',
      host: 'my-domain.waylay.io',
      path: `/api/sensors/${name}/versions?includeDeprecated=true`
    })

    done()
  })
})

test('create', done => {
  const script = { name: 'my-sensor' }

  waylay.sensors.create(script)

  moxios.wait(() => {
    const { config, url } = moxios.requests.mostRecent()

    expect(config).toMatchObject({
      method: 'post',
      data: JSON.stringify(script)
    })
    expect(Url.parse(url)).toMatchObject({
      protocol: 'https:',
      host: 'my-domain.waylay.io',
      path: `/api/sensors`
    })

    done()
  })
})

test('remove', done => {
  const name = 'my-script'
  waylay.sensors.remove(name)

  moxios.wait(() => {
    const { config, url } = moxios.requests.mostRecent()

    expect(config).toMatchObject({ method: 'delete' })
    expect(Url.parse(url)).toMatchObject({
      protocol: 'https:',
      host: 'my-domain.waylay.io',
      path: `/api/sensors/${name}`
    })

    done()
  })
})

test('remove force', done => {
  const name = 'my-script'
  waylay.sensors.remove(name, { force: true })

  moxios.wait(() => {
    const { config, url } = moxios.requests.mostRecent()

    expect(config).toMatchObject({ method: 'delete' })
    expect(Url.parse(url)).toMatchObject({
      protocol: 'https:',
      host: 'my-domain.waylay.io',
      path: `/api/sensors/${name}?force=true`
    })

    done()
  })
})

test('getScript', done => {
  const { name, version } = { name: 'my-script', version: '1.0.0' }
  waylay.sensors.getScript(name, version)

  moxios.wait(() => {
    const { config, url } = moxios.requests.mostRecent()

    expect(config).toMatchObject({ method: 'get' })
    expect(Url.parse(url)).toMatchObject({
      protocol: 'https:',
      host: 'my-domain.waylay.io',
      path: `/api/sensors/${name}/versions/${version}/script`
    })

    done()
  })
})

test('getVersions', done => {
  const name = 'my-script'
  waylay.sensors.getVersions(name)

  moxios.wait(() => {
    const { config, url } = moxios.requests.mostRecent()

    expect(config).toMatchObject({ method: 'get' })
    expect(Url.parse(url)).toMatchObject({
      protocol: 'https:',
      host: 'my-domain.waylay.io',
      path: `/api/sensors/${name}/versions`
    })

    done()
  })
})

describe('_getEndpoint', () => {
  const registryBaseUrl = 'https://registry.waylay.io'

  test.each([
    ['foobar', `${registryBaseUrl}/foobar`],
    ['/foobar', `${registryBaseUrl}/foobar`],
    ['foobar/', `${registryBaseUrl}/foobar/`],
    ['foo/bar', `${registryBaseUrl}/foo/bar`]
  ])('with registry - %s', (path, expected) => {
    let waylay = new Waylay({
      registry: {
        baseUrl: registryBaseUrl
      }
    })

    const sensorsEndpoint = waylay.sensors._getEndpoint(path)

    expect(sensorsEndpoint).toHaveProperty('url', expected)
  })
})
